# 后端 HTTP API 地址。留空表示使用当前页面同源地址。
VITE_API_BASE=

# 后端 WebSocket 地址。留空时自动根据 VITE_API_BASE 推导，VITE_API_BASE 也为空则使用当前页面同源地址。
VITE_WS_BASE=

# 示例：
# VITE_API_BASE=http://127.0.0.1:8081
# VITE_WS_BASE=ws://127.0.0.1:8081